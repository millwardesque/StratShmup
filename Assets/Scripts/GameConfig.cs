using UnityEngine;
using System.Collections;

public class GameConfig {
    public static float TargetDistanceThreshold = 0.2f; // Minimum distance to target at which point we can say we've 'reached' it.
}
